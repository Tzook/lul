'use strict';
let ServicesBase = require('../master/master.services.js');

/**
 * Socketio's services
 */
class SocketioServices extends ServicesBase {
    /**
	 * Initializes the instance
	 */
	init(files, app) {
		
	}
}

module.exports = SocketioServices;