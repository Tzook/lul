'use strict';
let RouterBase = require('../master/master.router.js');

/**
 * Template's router
 */
class TemplateRouter extends RouterBase {
	/**
	 * Initializes the instance
	 */
	init(files, app) {
		super.init(files, app);   
	}
	
	initRoutes(app) {
		
	}
}

module.exports = TemplateRouter;