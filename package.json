{
    "name": "lel",
    "version": "0.0.1",
    "description": "very lel..",
    "main": "main.js",
    "scripts": {
        "// b explanation": "not sure what it's needed for atm, but let's keep it here",
        "b": "git checkout master && git merge development && node output\\lib\\scripts\\cloneDb.js && git push origin master && git checkout development",
        "s": "gulp watch",
        "// start-explanation": "1) verify n exists. 2) verify node latest version exists while keeping the machine's original node version installed. 3) start app with node latest version",
        "start": "INSTALL_V=12.14.1 && ([ -a /usr/local/bin/n ] || sudo npm i -g n) && ([ -d /usr/local/n/versions/node/$INSTALL_V ] || (CURRENT_V=$(node -v) && sudo n $INSTALL_V && sudo n $CURRENT_V)) && n use $INSTALL_V node_modules/.bin/gulp watch --color",
        "// test-explanation": "1) start app in the background and save its process id. 2) run tests in tests folder. 3) kill app with saved process id.",
        "test": "gulp & SERVER_PID=$!; (cd tests; npm i && node run-tests.js); kill $SERVER_PID",
        "// postinstall-explanation": "this task is run by heroku after deploying, so it compiles the output files",
        "postinstall": "gulp compile"
    },
    "dependencies": {
        "@octokit/rest": "17.0.0",
        "@types/express": "4.17.3",
        "@types/faker": "4.1.10",
        "@types/mongoose": "5.7.3",
        "@types/node": "13.9.0",
        "@types/socket.io": "2.1.4",
        "@types/underscore": "1.9.4",
        "body-parser": "1.19.0",
        "compression": "1.7.4",
        "connect-mongo": "3.2.0",
        "cookie-parser": "1.4.4",
        "cors": "2.8.5",
        "express": "4.17.1",
        "express-session": "1.17.0",
        "faker": "4.1.0",
        "forever": "2.0.0",
        "gulp": "4.0.2",
        "gulp-develop-server": "0.5.2",
        "gulp-sourcemaps": "2.6.5",
        "gulp-typescript": "5.0.1",
        "heroku-client": "3.1.0",
        "mongoose": "5.9.3",
        "passport": "0.4.1",
        "passport-local": "1.0.0",
        "passport.socketio": "3.7.0",
        "q": "1.5.1",
        "socket.io": "2.0.4",
        "typescript": "3.8.3",
        "underscore": "1.12.1",
        "winston": "3.2.1",
        "winston-mongodb": "5.0.1"
    },
    "engines": {
        "node": "12.14.1",
        "npm": "6.13.4"
    },
    "license": "MIT",
    "//": "No devDependencies. Everything needed for compilation is in dependencies, because heroku compiles the files",
    "devDependencies": {}
}
