{
  "name": "lel",
  "version": "0.0.1",
  "description": "very lel..",
  "main": "main.js",
  "scripts": {
    "build": "git checkout master && git merge development && git checkout development",
    "s": "gulp watch",
    "// start-explanation": "1) verify n exists. 2) verify node latest version exists while keeping the machine's original node version installed. 3) start app with node latest version",
    "start": "INSTALL_V=8.11.1 && ([ -a /usr/local/bin/n ] || sudo npm i -g n) && ([ -d /usr/local/n/versions/node/$INSTALL_V ] || (CURRENT_V=$(node -v) && sudo n $INSTALL_V && sudo n $CURRENT_V)) && n use $INSTALL_V node_modules/.bin/gulp watch --color",
    "// test-explanation": "1) start app in the background and save its process id. 2) run tests in tests folder. 3) kill app with saved process id.",
    "test": "gulp & SERVER_PID=$!; (cd tests; npm i && node run-tests.js); kill $SERVER_PID",
    "// postinstall-explanation": "this task is run by heroku after deploying, so it compiles the output files",
    "postinstall": "gulp compile"
  },
  "dependencies": {
    "@octokit/rest": "15.2.5",
    "@types/express": "4.0.39",
    "@types/faker": "4.1.2",
    "@types/mongoose": "4.7.28",
    "@types/node": "8.0.57",
    "@types/socket.io": "1.4.31",
    "@types/underscore": "1.8.5",
    "body-parser": "1.18.2",
    "compression": "1.7.2",
    "connect-mongo": "2.0.1",
    "cookie-parser": "1.4.3",
    "cors": "2.8.4",
    "express": "4.16.3",
    "express-session": "1.15.6",
    "faker": "4.1.0",
    "forever": "0.15.3",
    "gulp": "3.9.1",
    "gulp-develop-server": "0.5.2",
    "gulp-sourcemaps": "2.6.4",
    "gulp-typescript": "4.0.2",
    "heroku-client": "3.0.6",
    "mongoose": "5.0.15",
    "passport": "0.4.0",
    "passport-local": "1.0.0",
    "passport.socketio": "3.7.0",
    "q": "1.5.1",
    "socket.io": "2.0.4",
    "typescript": "2.8.3",
    "underscore": "1.9.0",
    "winston": "2.4.2",
    "winston-mongodb": "3.0.1"
  },
  "engines": {
    "node": "8.11.1",
    "npm": "5.5.1"
  },
  "license": "MIT",
  "//": "No devDependencies. Everything needed for compilation is in dependencies, because heroku compiles the files",
  "devDependencies": {}
}
